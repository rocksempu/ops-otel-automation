name: Criar Dashboard (Self-Service)

on:
  workflow_dispatch:
    inputs:
      template_type:
        type: choice
        description: 'Modelo de Dashboard'
        options:
        - goldensignals
        - detalhesporservico
        - rum
        required: true
      
      # Inputs de Governança Obrigatória (Conforme MVP)
      service_name:
        description: 'Nome do Serviço (Ex: ibpf-api)'
        required: true
      service_namespace:
        description: 'Namespace (Ex: pix, crm)'
        required: true
      service_owner:
        description: 'Owner (Squad/Email)'
        required: true
      environment:
        type: choice
        description: 'Ambiente'
        options: [dev, hml, prd]
        required: true
      service_version:
        description: 'Versão (SemVer)'
        default: '1.0.0'
        required: false
      
      # Controle de Alertas
      enable_alerts:
        description: 'Ativar Alertas? (Em PRD será sempre Ativado)'
        type: boolean
        required: false
        default: true

jobs:
  deploy-dashboard:
    runs-on: self-hosted
    name: "Criar: ${{ inputs.template_type }} para ${{ inputs.service_name }}"
    
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      # --- NOVO: QUALITY GATE (Linting) ---
      # Valida se os templates JSON estão com a sintaxe correta antes de aplicar.
      # Usa o Python que já temos instalado.
      - name: 🛡️ Quality Gate - Validar JSONs
        run: |
          $pythonPath = "C:\Users\FabioPaixao\AppData\Local\Programs\Python\Python313\python.exe"
          $files = Get-ChildItem "templates\*.json"
          
          foreach ($file in $files) {
            Write-Host "Validando: $($file.Name)..."
            & $pythonPath -m json.tool "$($file.FullName)" > $null
            
            if ($LASTEXITCODE -ne 0) {
              Write-Error "❌ JSON Inválido: $($file.Name)"
              exit 1
            }
          }
          Write-Host "✅ Todos os templates estão válidos!"

      # --- FIM DO QUALITY GATE ---

      - name: Terraform Init
        working-directory: ./terraform
        run: '& "C:\Terraform\terraform.exe" init'

      - name: Terraform Apply
        working-directory: ./terraform
        env:
          TF_VAR_grafana_url: ${{ secrets.GRAFANA_URL }}
          TF_VAR_grafana_auth: ${{ secrets.GRAFANA_TOKEN }}
        run: |
          & "C:\Terraform\terraform.exe" apply -auto-approve `
            -var="template_type=${{ inputs.template_type }}" `
            -var="service_name=${{ inputs.service_name }}" `
            -var="service_namespace=${{ inputs.service_namespace }}" `
            -var="service_owner=${{ inputs.service_owner }}" `
            -var="service_version=${{ inputs.service_version }}" `
            -var="environment=${{ inputs.environment }}" `
            -var="enable_alerts=${{ inputs.enable_alerts }}"